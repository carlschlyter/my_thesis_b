import React from 'react'

export default function Test() {
    return (
        <React.Fragment>
            <h1>Create a test entry in the database</h1>
            <p>Let's see if we can make it work to use the create api...</p>
        </React.Fragment>
    )
}
